You are a Senior UI Developer standardizing an empty state layout.

FILES TO AUDIT:
- frontend/src/components/SalespersonRegistry.tsx (or the main Salesperson list component)

REQUIRED ACTIONS:
1. Locate the block of code rendering the "NO SALESPERSONS FOUND" message.
2. Replace it with a layout that exactly mirrors the Project module's empty state.
3. It should use a Flexbox column layout, centered, with a soft gray background or padding.
4. Include an icon (e.g., `Inbox` or `SearchX` from `lucide-react`, styled with a soft gray background block).
5. Add a bold title: "No Matches Found" (text-slate-800 or similar).
6. Add a subtitle: "We couldn't find any salespersons matching '[search term]'. Try a different term." (text-slate-500).
7. Ensure it displays conditionally only when the search yields zero results.

OUTPUT: Provide the updated empty state JSX block.