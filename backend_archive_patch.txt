You are a Senior UI/UX Developer. We need to redesign the ProjectFormModal to exactly match the clean, simple aesthetic of our Salesperson form.

FILES TO AUDIT:
- frontend/src/components/ProjectFormModal.tsx

REQUIRED ACTIONS:
1. HEADER: 
   - Make the top header simple and clean: A white background, a dark title ("New Project Registration" or "Edit Project"), and a simple X button to close. Remove any heavy gray tab backgrounds or pills.
2. FORM SECTIONS (The "Salesperson" Style):
   - Remove ALL nested gray background boxes (e.g., `bg-slate-50`, `bg-slate-100`, rounded cards) inside the form body. The entire form background must be purely white.
   - Replace the old dark/gray section titles with clean blue ones: `<h3 className="text-lg font-semibold text-blue-600 border-b border-blue-200 pb-2 mb-4 mt-6">Section Name</h3>` (Apply this to "Project Details", "Location", "Technical Details", etc.).
   - Ensure all input fields use a standard, simple border (`border-slate-300 rounded-md`) without heavy shadows.
3. ACTION BUTTONS:
   - Remove the heavy "floating dock" style container at the bottom.
   - Place the "Cancel" and "Save Project" buttons inline at the very bottom of the form area, aligned to the right, matching standard modal footers.

OUTPUT: Provide the completely updated `ProjectFormModal.tsx` code with the simplified UI. Ensure no functional logic or state variables are removed, ONLY change the Tailwind CSS classes.