You are a Senior UI Developer updating a beta banner to remain sticky during scroll.

FILES TO AUDIT:
- frontend/src/App.tsx (or the main Layout wrapper component)

REQUIRED ACTIONS:
1. Locate the red Beta Testing banner `<div>` at the very top of the layout.
2. Update the Tailwind classes to make it stick to the top of the viewport. 
3. Replace the word `relative` with `sticky top-0`.
4. Ensure the `z-[9999]` class is still there so it floats above the scrolling content, sidebars, and any modals.
5. Example complete className: `"sticky top-0 w-full bg-red-600 text-white text-center text-[10px] sm:text-xs font-black uppercase tracking-[0.2em] py-1.5 z-[9999] shadow-md"`

OUTPUT: Provide the updated JSX block containing the banner.