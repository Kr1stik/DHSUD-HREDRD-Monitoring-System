You are a Senior UI/UX Developer fixing a layout bug and a broken image source.

FILES TO AUDIT:
- frontend/src/components/SalespersonFormModal.tsx

REQUIRED ACTIONS:
1. FIX THE IMAGE SOURCE:
   - Locate the `<img src={salesperson.photo} ... />` tag in the View Mode section.
   - Change the `src` to ensure it points to the backend if it's a relative path. Update it to:
     `src={salesperson.photo.startsWith('http') ? salesperson.photo : \`http://localhost:8000${salesperson.photo}\`}`
2. RESTRUCTURE THE MODAL LAYOUT:
   - The main white container must be: `className="bg-white rounded-xl shadow-2xl w-full max-w-4xl max-h-[90vh] flex flex-col overflow-hidden relative"`
   - Ensure the Header (the title and X button) is inside a fixed container: `className="flex-none p-6 border-b border-slate-200"`
   - Wrap ALL the actual form sections (Personal Info, Broker, etc.) inside a scrolling container: `className="flex-1 overflow-y-auto p-6"`
   - Move the Action Buttons (Cancel / Save / Close) completely OUT of the scrolling form area. Put them at the very bottom inside their own pinned container: `className="flex-none p-4 border-t border-slate-200 bg-slate-50 flex justify-end gap-3"`

OUTPUT: Provide the completely restructured return block of SalespersonFormModal.tsx.
