You are a Senior Django Developer configuring JWT token lifetimes for an office environment.

FILES TO AUDIT:
- housingProject/settings.py

REQUIRED ACTIONS:
1. Locate the `SIMPLE_JWT` configuration dictionary in your settings. (If it doesn't exist, create it below your REST_FRAMEWORK settings).
2. Ensure you have `from datetime import timedelta` imported at the top of the file.
3. Update or set the token lifetimes so the user doesn't get logged out mid-shift:
   `SIMPLE_JWT = {`
   `    'ACCESS_TOKEN_LIFETIME': timedelta(days=1),`
   `    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),`
   `    # ... keep any other existing settings here`
   `}`

OUTPUT: Provide the updated SIMPLE_JWT dictionary block.