You are a Senior UI Developer fixing mobile overflow and reverting accidental dark theme classes.

FILES TO AUDIT:
- frontend/src/components/CloudBackup.tsx

REQUIRED ACTIONS:
1. REVERT COLORS: Search the main wrappers for any accidental `bg-slate-800` or `bg-slate-900` classes and remove them so the component inherits the standard light background of the dashboard.
2. FIX EDGE OVERFLOW: Locate the outermost container of this component. Add `px-4 sm:px-6 py-6` to ensure there is breathing room on mobile screens and the content doesn't touch the phone bezels.
3. FIX CARD WIDTH: Ensure the main white card container has `w-full max-w-xl mx-auto box-border` so it scales down perfectly.

OUTPUT: Provide the updated JSX for the main layout wrappers.