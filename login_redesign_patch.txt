You are a Senior UI Developer redesigning a Login page into a modern split-screen card layout.

FILES TO AUDIT:
- frontend/src/components/Login.tsx (or LoginPage.tsx)

REQUIRED ACTIONS:
1. Wrap the entire page in a centered container: `min-h-screen flex items-center justify-center bg-slate-100 p-4`.
2. Create the main card: `flex flex-col md:flex-row w-full max-w-4xl bg-white rounded-2xl shadow-2xl overflow-hidden`.
3. LEFT PANEL (The Form): `w-full md:w-1/2 p-8 md:p-12 flex flex-col justify-center bg-white`.
   - Top of left panel: Add a placeholder for the logo (`<img src="/logo.png" alt="DHSUD Logo" className="h-16 w-16 mb-4" />`) and the text: "Department of Human Settlements and Urban Development (DHSUD)" and "HREDRD - NIR".
   - Form Fields: Username/Email and Password. (Make inputs clean with light gray borders).
   - Add a "Remember Me" checkbox.
   - STRIP OUT any "Forgot Password" links.
   - Update the "Sign In" button to be a bold, solid color (e.g., `bg-blue-600 text-white rounded-full py-3`).
4. RIGHT PANEL (The Branding): `hidden md:flex w-1/2 bg-gradient-to-br from-blue-600 to-blue-800 p-12 flex-col justify-center items-center text-center text-white`.
   - Title: "Welcome to HREDRD System"
   - Subtitle: "Regional Data Management & Archiving"
   - STRIP OUT any "Sign Up" buttons or text.

OUTPUT: Provide the fully updated Login component JSX.