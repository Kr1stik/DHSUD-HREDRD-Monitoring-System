You are a Senior Backend Engineer. Fix the hanging port issue in the Google OAuth flow when a user closes the browser tab.

FILES TO AUDIT:
- trackerApp/drive_service.py

REQUIRED UPDATES:
1. MULTI-PORT FALLBACK:
   - In `get_drive_service()`, define a list of fallback ports: `[8088, 8089, 8090]`.
   - Wrap the `flow.run_local_server()` call in a `for port in ports:` loop.
   - Try to run the server on the current port. If it succeeds, `break` the loop and proceed.
   - If it raises an `OSError` or `socket.error` (meaning the port is stuck from a previously closed tab), `continue` to the next port.
   - If the loop exhausts all ports, raise an Exception("All local ports are blocked. Please restart the application.")
   - Keep the existing `prompt='select_account'` and `access_type='offline'` arguments.

OUTPUT: Provide the updated `drive_service.py` file with this robust fallback logic.