You are a Senior React Developer fixing broken image file references.

FILES TO AUDIT:
- Scan through all components, especially `frontend/src/components/Login.tsx`, `Sidebar.tsx`, or `Navbar.tsx`.

REQUIRED ACTIONS:
1. Search for any `<img src="/DHSUD LOGO.png"` or `<img src="DHSUD LOGO.png"`.
2. Change the `src` attribute to strictly read `src="/logo.png"`.

OUTPUT: List the files modified and the updated image tags.