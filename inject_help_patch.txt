You are a Senior React Developer injecting a global component into the main app layout.

FILES TO AUDIT:
- frontend/src/App.tsx

REQUIRED ACTIONS:
1. Import the new component at the top: `import FloatingHelp from './components/FloatingHelp';`
2. Locate your main layout wrapper (likely inside the `AppContent` function, possibly right next to where the `<Sidebar />` and the `<Routes>` are rendered).
3. Add the `<FloatingHelp />` component just inside the main layout container so it renders globally across all routes.

OUTPUT: Provide the updated `App.tsx` file.