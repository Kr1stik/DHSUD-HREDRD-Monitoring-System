You are a Senior React Developer removing a hardcoded print footer from a specific file.

FILES TO AUDIT:
- frontend/src/components/ProjectRegistry.tsx

REQUIRED ACTIONS:
1. Scroll to the very bottom of the component's JSX, specifically inside the print layout section.
2. Look below the signature lines ("PREPARED BY:" and "APPROVED BY:").
3. Find the container (usually a `<div>` with border styles and small text) that holds the strings "DHSUD-NIR-HREDRD-V1.0" and "GENERATED VIA MONITORING SYSTEM".
4. Completely DELETE that specific container from the JSX so the page ends immediately after the signatures.

OUTPUT: Provide the updated signature section showing the footer is completely gone.