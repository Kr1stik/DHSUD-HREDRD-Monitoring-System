You are a Senior React Developer fixing Vite build paths for a Vercel deployment.

FILES TO AUDIT:
- frontend/vite.config.ts (or vite.config.js)

REQUIRED ACTIONS:
1. Look at the `defineConfig` object.
2. If there is a `base: '/static/'` property (or anything similar), completely REMOVE it. 
3. The base should default to `'/'` or just be removed entirely so Vite builds standard absolute paths.

OUTPUT: Provide the updated vite.config file.